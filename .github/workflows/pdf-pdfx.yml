name: pdf-pdfx

permissions:
  contents: read
  actions: write

env:
  CARGO_TERM_PROGRESS_WHEN: never

on:
  push:
  pull_request:

jobs:
  pdfx-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pdfx
        run: cargo install pdfx --locked --quiet
      - name: Run pdfx
        run: |
          mkdir -p reports
          shopt -s nullglob
          status=0
          for pdf in docs/*.pdf; do
            if ! pdfx "$pdf" &> "reports/$(basename "$pdf").log"; then
              status=1
            fi
          done
          exit $status
      - name: Upload pdfx reports
        if: ${{ hashFiles('reports/*.log') != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: pdfx-reports
          path: reports/
